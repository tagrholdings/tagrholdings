psycopg[binary]>=3.2.10,<4
httpx>=0.27,<1
beautifulsoup4>=4.12,<5
openai>=1.54,<3
python-dotenv>=1.0,<2
# Only needed for the marketplace source (sources/marketplace/). Browsers are
# installed separately: `playwright install chromium` (the Dockerfile does it).
playwright>=1.47,<2
